<h2>Edit schedule</h2>

<%= render "form.html", changeset: @changeset,
                        action: schedule_path(@conn, :update, @schedule) %>

<%= link "Back", to: schedule_path(@conn, :index) %>
